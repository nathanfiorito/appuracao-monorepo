/** Quantidade máxima de QR Codes por boletim (formato TSE). */
export const MAX_QRS_POR_BOLETIM = 4

/** Caminho do endpoint de ingestão de boletins. */
export const INGESTAO_PATH = '/boletins'

/** Caminho base dos resultados por município (sufixo: /{codigo_municipio}.json). */
export const RESULTADOS_PATH = '/resultados'
